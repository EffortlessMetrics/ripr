# Workflow run-block exceptions.
#
# Format:
# path|max_non_empty_lines|reason
#
# Workflows should orchestrate Cargo, xtask, npm, and release actions rather
# than embed complex shell programs. This file allows known workflow blocks
# while keeping the budget visible.

.github/workflows/ci.yml|3|CI run blocks call direct Cargo or npm commands.
.github/workflows/coverage.yml|1|Coverage workflow runs one cargo-llvm-cov command.
.github/workflows/publish-extension.yml|20|Release workflow includes marketplace packaging and publishing orchestration.
.github/workflows/release-server-binaries.yml|30|Release workflow packages platform server binaries.
.github/workflows/security.yml|0|Security workflow uses standard cargo-deny and dependency-review actions without shell run blocks.
