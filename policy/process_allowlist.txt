# Allowlisted process-spawn surfaces.
#
# Format:
# path|pattern|max_count|owner|reason
#
# Process spawning belongs in adapters, smoke tests, and xtask. New occurrences
# must name the owner and reason instead of drifting into domain or analysis
# logic unnoticed.

crates/ripr/src/agent/artifact.rs|Command::new|1|repair-artifact-provenance|RIPR-SPEC-0134: artifact validation resolves the actual repository HEAD and tracked-worktree state through bounded git probes before accepting provenance-bound evidence; no arbitrary command execution.
crates/ripr/src/agent/artifact.rs|use std::process::Command|1|repair-artifact-provenance|RIPR-SPEC-0134: artifact validation imports Command for the bounded git provenance probes described above.
crates/ripr/src/agent/loop_commands.rs|Command::new|3|agent/advisory-commands|#2347: the bash argv encoder is proved by executing the encoded token in a real bash — a round-trip transcript over hostile values, a redirect-clobber guard, and a usability probe that rejects a WSL bash which cannot open host paths. Test module only; no production spawn.
crates/ripr/src/analysis/diff/load.rs|Command::new|7|analysis/diff|RIPR-SPEC-0084: git diff adapter resolves the real default base branch (symbolic-ref, origin/main, origin/master, local main/master) and fail-closes with a named message; production helpers (run_git_diff, git_symbolic_ref_quiet, git_ref_exists) plus test module temp-repo setup. RIPR-SPEC-0112: working_tree_has_tracked_changes (git status --porcelain) discloses when --base excludes uncommitted edits.
crates/ripr/src/analysis/diff/load.rs|use std::process::Command|2|analysis/diff|Git diff adapter and its test module both import Command directly.
crates/ripr/src/analysis/language/rust.rs|Command::new|5|language-adapter-swarm|test fixture recognizes Cargo binary subprocess invocation
crates/ripr/src/analysis/probes/subprocess.rs|Command::new|7|analysis/probes|RIPR-SPEC-0178: bounded subprocess adapter recognizer inspects source text and fixture bodies for one literal allowlisted command; it does not execute a subprocess or broaden the runtime command surface.
crates/ripr/src/analysis/seam_cache.rs|Command::new|2|analysis/seam-cache|#3037: the cargo-metadata differential test probes `cargo --version` (fail-closed when cargo is unavailable; explicit RIPR_ALLOW_MISSING_CARGO opt-out) and runs `cargo metadata --no-deps --offline` on a synthetic temp workspace to check the typed path-dependency edge set against Cargo. Test module only; no production spawn.
crates/ripr/src/analysis/workspace/cargo_targets.rs|Command::new|2|harness-metadata-validation|#3634: one bounded `cargo metadata --no-deps --offline` probe per batch (cwd anchored to the analyzed root, stdout through a temp file, shared deadline-aware poll, fail-closed to manifest_unavailable when cargo or metadata is unavailable). The second spawn is the test-only Windows junction alias (`cmd /C mklink /J`) for the alias-form inventory pin (#3637 review); it runs in the test module only.
crates/ripr/src/app/check.rs|Command::new|1|perl-managed-producer|Campaign 31 Phase D (#1407): managed producer mode invokes perllsp --ripr-facts to generate a fact packet. Explicit opt-in only ([perl] producer = "perllsp"); no silent invocation.
crates/ripr/src/app/check.rs|use std::process::Command|1|perl-managed-producer|Managed producer mode imports Command for the perllsp invocation.
crates/ripr/src/app/check_artifact/tests.rs|Command::new|2|app/check-artifact|RIPR-SPEC-0140 (#2251): worktree artifact-reuse tests initialize temp git repos and mutate tracked files to prove reuse fails closed on diff_bytes_hash when the worktree drifts.
crates/ripr/src/app/pr_evidence.rs|Command::new|1|pr-evidence|Campaign 31 item 8d: pr-evidence runs git commands (verify_revision, changed_files, write_diff) via Command::new("git"). No subprocess to cargo; check_workspace is called directly.
crates/ripr/src/app/pr_evidence.rs|use std::process::Command|1|pr-evidence|pr-evidence imports Command for git invocations only.
crates/ripr/src/app/receipt.rs|Command::new|2|receipt-head-binding|#1941: receipt writer validates --current-head against git rev-parse HEAD at --root to prevent SHA fabrication on repair evidence; #2329 test helper creates a temp git repo (init/config/add/commit) to exercise the binding end-to-end.
crates/ripr/src/app/repair_attempt.rs|Command::new|2|agent-repair|#2927/#3163: repair-attempt orphan-cleanup fixture and receipt binding use bounded git diff/ls-files probes to fail closed on out-of-surface rewrites; no arbitrary command execution.
crates/ripr/src/app/repair_attempt.rs|use std::process::Command|1|agent-repair|#3163: receipt binding imports Command solely for bounded trusted-HEAD edit-surface probes.
crates/ripr/src/app/review_comments.rs|Command::new|2|review-comments-producer-admission|#1660: producer admission resolves the exact base/head commit and tree through bounded read-only git probes; no arbitrary command execution.
crates/ripr/src/app/ripr_plus.rs|Command::new|1|ripr-plus|Campaign 31 item 8f: ripr plus runs `git rev-parse HEAD` to stamp the receipt with the current commit. No subprocess to cargo.
crates/ripr/src/app/verification_execution.rs|Command::new|1|verification-execution|#2332: explicit opt-in producer-owned ripr agent verify execution uses one direct child process and bounded git currentness probes; no shell and no inherited environment.
crates/ripr/src/cli/commands.rs|Command::new|2|cli/review-comments|Review-comments diff loading and its bounded producer-backed integration fixture probe git directly; neither path executes arbitrary commands.
crates/ripr/src/cli/commands/doctor.rs|Command::new|4|cli/doctor|Doctor family extraction (#2271) moved these from commands.rs: Perl runner availability (prove/yath via where/which); Campaign 31 item 5 Perl preview also probes the perllsp/perl-lsp producer version (Command::new(candidate).--version) and resolves its absolute path via which/where.
crates/ripr/src/git.rs|Command::new|6|shared-git-helper|#1921/#2430/#2860: unified git invocation helper with one published-crate git spawn point, Windows taskkill process-tree cleanup for timed-out descendants, and test-only re-exec/descendant harnesses for deterministic deadline and pipe-drain coverage.
crates/ripr/src/lsp/tests.rs|Command::new|2|lsp-test-scoping|Issue #2130: the LSP production-scoping integration fixtures build a real git repo (init/add/commit) so diff analysis runs against committed state; test-only setup spawn with fail-closed error reporting. Second spawn: the changed-test-file exclusion regression test (#2137) builds its own temp git repo through the same helper.
crates/ripr/src/lsp/tests.rs|use std::process::Command|1|lsp-test-scoping|Issue #2130: LSP production-scoping test fixtures import Command for temp-git-repo setup only.
crates/ripr/src/main.rs|Command::new|1|cli/panic-hook-test|#2660: child-process regression test executes the panic boundary under both RUST_BACKTRACE settings and verifies the ripr-formatted report plus exit code 2.
crates/ripr/src/output/doctor.rs|Command::new|1|output/doctor|Doctor core evaluation probes git/cargo/rustc availability via `Command::new(tool).arg("--version")`; advisory tool-availability check, no analysis.
crates/ripr/src/output/first_pr.rs|Command::new|4|first-pr/front-door-polish|First-pr setup preflight validates git worktree, base/head refs, and diff range before assigning repair work; unit tests initialize temp git repos for those fail-closed packets.
crates/ripr/src/output/first_pr.rs|use std::process::Command|1|first-pr/front-door-polish|First-pr setup preflight imports Command for bounded git probes and temp-git test setup.
crates/ripr/src/output/markdown.rs|Command::new|3|output/powershell-proof|#1672: the native pwsh argv/artifact proof compiles a recorder fixture with rustc, probes the pwsh instrument, and runs translated lines under real pwsh. Test module only; no production spawn.
crates/ripr/src/output/review_comments_receipt.rs|Command::new|1|review-comments-receipt|Review-comments receipts resolve the requested base/head revisions with a bounded git rev-parse probe so cache identity and terminal receipts are tied to the actual repository revisions.
crates/ripr/tests/boundary_gap_closure.rs|Command::new|3|boundary-gap-closure|#3165/#3167: the isolated real-binary product journey creates a temporary Git repository, runs the worktree-built ripr check before and after the exact equality test, and runs ripr outcome to prove the same seam closes; no shell, network, or user-repository mutation.
crates/ripr/tests/cli_help_hierarchy.rs|Command::new|1|cli-tests|#2930: rendered-help drift tests run the built ripr binary (`--help`, `help --all`, `agent --help`) to pin the canonical command hierarchy against actual output rather than source text.
crates/ripr/tests/cli_help_hierarchy.rs|use std::process::Command|1|cli-tests|#2930: rendered-help drift tests import Command to run the built ripr binary.
crates/ripr/tests/cli_smoke.rs|Command::new|13|cli-tests|#3158/#3159: smoke tests run the built CLI binary directly, with env-var overrides, and through the isolated installed-binary version contract; Evidence: isolated_installed_binary_version_contract_is_side_effect_free records exact stdout/stderr/status and stable filesystem snapshots; Review: revisit when the version-case spawn matrix changes. Existing coverage: RIPR-SPEC-0083, RIPR-SPEC-0112, and Campaign 31 item 8.
crates/ripr/tests/generated_review_workflow.rs|Command::new|1|github-review-workflow|#2621: generated workflow integration test runs the worktree-built ripr binary in an isolated temporary directory to verify the emitted batching contract; no user repository is mutated.
crates/ripr/tests/generated_review_workflow.rs|use std::process::Command|1|github-review-workflow|#2621: integration harness import for the isolated generated-workflow binary invocation.
crates/ripr/tests/immutable_candidate_falsifier.rs|Command::new|3|input-qualification|RIPR-SPEC-0161: the #3279 falsifier corpus drives the built ripr binary and real git plumbing against unique two-commit fixture repositories; every spawn is the integration harness itself, no production analysis code spawns. Evidence: the 7-test corpus in the same file; Review: revisit when the corpus adds new spawn shapes.
crates/ripr/tests/immutable_candidate_falsifier.rs|use std::process::Command|1|input-qualification|RIPR-SPEC-0161: import for the corpus harness described above.
crates/ripr/tests/lsp_lifecycle.rs|Command::new|3|lsp-lifecycle-conformance|Issue #2030: the lifecycle conformance harness spawns the worktree-absolute ripr binary under test once per session (ripr lsp --stdio); every session guard kills and reaps the child so no server survives the suite. Issue #1930: the compatibility-command journey builds a temp git fixture root (init/add/commit helper, one Command::new site) and probes the binary under test with ripr --version for the harness receipt (one site).
crates/ripr/tests/mcp_stdio.rs|Command::new|1|mcp-stdio-harness|#3088: the bounded MCP stdio integration harness launches the worktree-built ripr binary to prove protocol-clean stdout, lifecycle behavior, framing limits, and root redaction; no shell or arbitrary command execution.
crates/ripr/tests/perl_two_binary_harness.rs|Command::new|3|perl-two-binary-harness|Campaign 31 item 3: the perllsp-to-ripr two-binary proof harness spawns the external perllsp producer (run_producer), the worktree-absolute ripr binary under test (run_ripr), and the which()/where lookup (which). Producer is opt-in via PATH; the harness skips cleanly when perllsp is absent.
crates/ripr/tests/perl_two_binary_harness.rs|use std::process::Command|1|perl-two-binary-harness|The two-binary harness imports Command for the perllsp, ripr, and which/where spawns.
crates/ripr/tests/python_repair_attempt.rs|Command::new|5|python-repair-driver-binding|#3569/#3570: the end-to-end driver binding and verification case matrices spawn the built ripr binary and real git plumbing against unique fixture repositories to prove prepare/apply binding, staleness, cage escapes, the four durable states, bounded verification execution, and the rollback proof; no shell, network, or user-repository mutation.
editors/vscode/src/client.ts|child_process|1|lsp/editor|VS Code client invokes ripr context through the resolved server command.
editors/vscode/src/client.ts|cp.execFile|1|lsp/editor|VS Code client invokes ripr context without shell expansion.
editors/vscode/src/downloader.ts|child_process|1|lsp/editor|Downloader probes the downloaded server executable version.
editors/vscode/src/downloader.ts|cp.execFile|1|lsp/editor|#1641: downloader probes the downloaded executable version without shell expansion; archive extraction no longer spawns platform tools.
editors/vscode/src/lspCompatibility.ts|child_process|1|lsp/editor|#1745 / RIPR-SPEC-0129: the shared version/LSP probe owner spawns only the selected ripr candidate through a bounded platform process-tree controller.
editors/vscode/src/lspCompatibility.ts|cp.spawn|3|lsp/editor|#1745 / RIPR-SPEC-0129: one POSIX process-group spawn, one Windows kill-on-close Job Object wrapper spawn, and the idempotent taskkill fallback own every version/LSP terminal path; no protocol payload reaches a shell.
editors/vscode/test/runTest.ts|child_process|1|vscode-packaged-integration|#2528: packaged Electron proof creates an isolated external Git fixture and launches the opt-in trust-boundary host through bounded child-process calls; no shell expansion.
editors/vscode/test/suite/catalog_admission.test.ts|child_process|1|distribution-admission-test|#1643: admission tests execute the compiled admission script in the real node process without shell expansion to prove admit/reject dispositions end to end; test module only, no production spawn.
editors/vscode/test/suite/lsp_compatibility.test.ts|child_process|1|lsp/editor-test|#1745 / RIPR-SPEC-0129: test-only fake servers spawn a long-lived descendant to prove version and LSP cleanup after timeout, failure, and direct-parent exit.
editors/vscode/test/suite/lsp_compatibility.test.ts|cp.spawn|1|lsp/editor-test|#1745 / RIPR-SPEC-0129: the retained process-tree oracle creates one test-only descendant and asserts its PID is dead after each probe disposition.
xtask/src/cargo_driver_tests.rs|Command::new|1|xtask-driver-lifecycle|#1687: embedded no-dependency fixture driver invokes the compiler-selected Cargo binary to rebuild only its isolated workspace while the driver remains alive; the existing bounded capture owner controls the outer fixture process and no production spawn owner changes.
xtask/src/cargo_driver_tests.rs|use std::process::Command|1|xtask-driver-lifecycle|#1687: Command import belongs to the embedded native Cargo lifecycle fixture source, not the xtask production module; bounded fixture execution covers success, gate failure and Windows executable replacement controls.
xtask/src/driver.rs|Command::new|3|xtask-driver-relocation|#3699: Windows driver bootstrap re-executes the driver from a fresh relocated copy outside the replaceable build output, plus test harness propagation probes.
xtask/src/driver.rs|use std::process::Command|1|xtask-driver-relocation|#3699: Windows driver bootstrap imports Command for driver copy re-execution.
xtask/src/main.rs|Command::new|2|gate-runner|#2343: diff-aware gate runner reads changed files via git diff. #1682: vscode-package distribution admission runs the compiled Node admission script (bounded argv, extension-dir cwd, no shell) before vsce packaging.
xtask/src/python_judged_panel_report/tests.rs|Command::new|1|test/panel-determinism|byte-stability pipeline copies the replay binary into its isolated fixture and exec-probes the copy with --version (bounded argv, no shell, no network) so a concurrent sibling cargo build cannot relink it between the two replay runs; test module only, no production spawn.
xtask/src/reports/back_sync.rs|Command::new|3|back-sync-verifier|RIPR-SPEC-0152: exact-object back-sync verification uses bounded Git probes to inspect commit parents, trees, ancestry, refs, and tags, plus synthetic temporary-repository tests; it does not execute product commands or mutate release repositories.
xtask/src/reports/back_sync.rs|use std::process::Command|1|back-sync-verifier|RIPR-SPEC-0152: back-sync verification imports Command solely for bounded Git observation and synthetic temporary-repository construction.
xtask/src/reports/first_hour.rs|Command::new|1|first-hour-harness|#1674 slice G: the installed standalone-LSP journey spawns the packaged ripr lsp --stdio server over real OS pipes with a generic framed client (bounded argv, no shell); the orphan budget terminates and refuses a lingering server.
xtask/src/reports/release_scope.rs|Command::new|2|release-scope|RIPR-SPEC-0145: candidate-only scope reconciliation invokes bounded git probes for current ref, commit, path, and preserved-path verification; no arbitrary command execution.
xtask/src/reports/release_scope.rs|use std::process::Command|1|release-scope|RIPR-SPEC-0145: release-scope imports Command for bounded Git observation only.
xtask/src/reports/source_promotion.rs|Command::new|7|source-promotion-preflight|RIPR-SPEC-0148: read-only source-promotion preflight invokes bounded Git probes in separately rooted source, swarm, and disposable repositories to verify exact parents, immutable ref identity, ancestry, changed paths, object availability, and dry-merge output; no shell or product command execution.
xtask/src/reports/source_promotion.rs|use std::process::Command|1|source-promotion-preflight|RIPR-SPEC-0148: source-promotion preflight imports Command for bounded Git observation and disposable-repository construction only.
xtask/src/reports/source_promotion_admission_fixture.rs|Command::new|4|release/source-promotion-admission|RIPR-SPEC-0177: source-owned synthetic fixtures use bounded git and current-xtask invocations to construct isolated repositories, run the production resolved-tree validator, and emit deterministic positive and J5-negative evidence without touching caller refs.
xtask/src/reports/source_promotion_admission_workflow.rs|Command::new|4|release/source-promotion-admission|RIPR-SPEC-0177: the trusted workflow harness uses bounded git observation/materialization and invokes the current source-owned controller executable without shell expansion; the Windows-only containment regression invokes cmd mklink /J to prove a profile-correct controller path cannot resolve outside the runner-owned workspace.
xtask/src/reports/source_promotion_admission_workflow.rs|use std::process::Command|1|release/source-promotion-admission|RIPR-SPEC-0177: the trusted workflow harness imports Command only for its bounded git and current-executable adapters.
xtask/src/reports/source_promotion_control/construction.rs|Command::new|1|source-promotion-controller|RIPR-SPEC-0177: exact-J construction invokes only the guarded unreferenced git commit-tree operation after terminal admission and qualification evidence.
xtask/src/reports/source_promotion_control/io.rs|Command::new|3|source-promotion-controller|RIPR-SPEC-0177: controller IO runs bounded process helpers and separately rejects symbolic or broken local candidate refs before no-deref mutation, without shell expansion.
xtask/src/reports/source_promotion_control/publication.rs|Command::new|1|source-promotion-controller|RIPR-SPEC-0177: candidate-ref publication invokes one exact-lease git push after local and remote authority rereads and reconciles remote state afterward.
xtask/src/reports/source_promotion_control/tests.rs|Command::new|1|release/source-promotion-tests|RIPR-SPEC-0177: the Windows-only alias regression invokes cmd mklink /J to create an unprivileged temporary directory junction and prove output containment resolves a real filesystem alias; Unix uses std symlink and no process.
xtask/src/reports/source_promotion_network_policy_resolution.rs|Command::new|10|release/source-promotion|Issue #1572 uses bounded Git object/index operations to reproduce the exact P0 preview and one-path policy tree, fingerprints the active Rust toolchain, then builds the checker from the exact source tree with a locked offline isolated target and runs it against the resolved tree; it creates immutable objects and disposable checkouts but moves no ref.
xtask/src/reports/source_promotion_verify.rs|Command::new|4|source-promotion-verifier|RIPR-SPEC-0149: exact-J verification uses bounded Git probes to validate parents, tree, ancestry, ranges, main reachability, and adversarial synthetic repositories; it does not execute product commands or mutate source release refs.
xtask/src/reports/source_promotion_verify.rs|use std::process::Command|1|source-promotion-verifier|RIPR-SPEC-0149: source-promotion verification imports Command solely for bounded Git observation and synthetic temporary-repository construction.
xtask/src/run.rs|Command::new|17|repo-policy|xtask run helpers are the repository command runner for Cargo, git, fixture execution, extension build orchestration, release upload orchestration, supply-chain report capture including exact binary stdout, bounded latency probes, byte-exact host-run receipt capture, stdout-to-file report inputs, cwd-bound packaged-release smoke, timeout-race regression coverage, Unix process-group termination, Windows process-tree termination, and Windows timeout regression helpers.
xtask/src/version.rs|Command::new|1|release-version-bump|#2711: gated version-bump validation invokes `cargo metadata --no-deps --format-version 1` after coordinated manifest updates and restores the originals on validation failure.
xtask/src/version.rs|use std::process::Command|1|release-version-bump|#2711: version-bump validation imports Command for the bounded cargo metadata probe only.
xtask/tests/help_hierarchy.rs|Command::new|1|xtask-help-tests|#3502: rendered-help integration tests execute the built xtask binary to verify bounded, exhaustive, per-command, and invalid-command help routing; no production subprocess is introduced.
xtask/tests/source_promotion_resolved_tree.rs|Command::new|1|source-promotion-resolved-tree-regression|RIPR-SPEC-0177: the synthetic J5 corpus initializes and indexes an isolated Git fixture, then invokes the built source-owned xtask network-policy checker against source-only, reconciled, duplicate, under-counted, raw-union orphan, and removal controls.
xtask/tests/source_promotion_workflow_contract.rs|Command::new|4|source-promotion-workflow-contract|RIPR-SPEC-0177: focused contract proof executes the exact inline jq verifier-receipt predicate and uses bounded git plus current-xtask run/verify processes to prove an authentic J5 rejection is a self-verifying workflow packet.
xtask/tests/spec_format_determinism.rs|Command::new|3|spec-format-determinism|#3455: the integration harness runs the built xtask command and creates one bounded temporary Git fixture with a pinned historical commit; no user repository is mutated.
xtask/tests/spec_format_determinism.rs|use std::process::Command|1|spec-format-determinism|#3455: the deterministic spec-format integration harness imports Command only for the bounded xtask and temporary-Git probes described above.
xtask/tests/spec_template_contract.rs|Command::new|1|spec-format-template-test|#3464: the integration harness runs the built xtask validator in an isolated temporary fixture; no user repository is mutated.
xtask/tests/spec_template_contract.rs|use std::process::Command|1|spec-format-template-test|#3464: the integration harness imports Command only for the isolated validator invocation.
