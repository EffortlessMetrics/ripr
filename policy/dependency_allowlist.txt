# Allowlisted dependency-management surfaces.
#
# Format:
# glob|kind|owner|reason
#
# New dependency manager files or manifests require an allowlist entry with an
# owner and reason. Keep dependency surfaces scoped to Cargo, the VS Code
# extension, and fixture input workspaces.
#
# Approved crate decisions:
# ra_ap_syntax|production_analyzer|analysis|ADR 0006 approves Rust-native
# parser-backed syntax extraction; add the Cargo dependency only in the scoped
# parser PR.

Cargo.lock|rust_workspace_lockfile|rust|Pinned Cargo dependency graph.
Cargo.toml|rust_workspace_manifest|rust|Root Cargo workspace manifest.
crates/**/Cargo.toml|rust_package_manifest|rust|Cargo package manifests.
editors/vscode/package-lock.json|editor_extension_lockfile|lsp/editor|Pinned npm dependency graph for the VS Code extension.
editors/vscode/package.json|editor_extension_manifest|lsp/editor|VS Code extension package manifest.
fixtures/**/Cargo.toml|fixture_input|analysis-fixtures|Fixture workspaces may contain Cargo manifests as analyzed inputs.
xtask/Cargo.toml|rust_tool_manifest|repo-policy|xtask package manifest.
