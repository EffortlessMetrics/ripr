# Allowlisted network surfaces.
#
# Format:
# path|pattern|max_count|owner|reason
#
# Network behavior must stay in explicit adapter or release surfaces. Unit tests
# and domain logic should not acquire hidden network dependencies.

.github/workflows/droid-review.yml|curl|1|ci/droid|Droid action bootstrap downloads a pinned GitHub CLI archive when the self-hosted runner does not provide gh.
.github/workflows/droid-security-scan.yml|curl|1|ci/droid|Droid action bootstrap downloads a pinned GitHub CLI archive when the self-hosted runner does not provide gh.
.github/workflows/droid.yml|curl|1|ci/droid|Droid action bootstrap downloads a pinned GitHub CLI archive when the self-hosted runner does not provide gh.
.github/workflows/publish-extension.yml|curl|1|release/ci|Release workflow polls GitHub Releases for the server manifest before publishing extension packages.
.github/workflows/server-archive-qualification.yml|curl|1|ci/release-qualification|Issue #3155 exact public ruleset detail read for active tag update/deletion protection; bounded retries, rate-limit and response-digest diagnostics, no token, artifact-only qualification, reviewed 2026-08-11.
.github/workflows/source-promotion-contract.yml|curl|1|release/source-promotion|Fixed public GitHub ruleset 20661783 provenance for source-promotion admission; evidence is the workflow jq predicate and review is required on contract changes.
crates/ripr/src/analysis/probes/subprocess.rs|curl|6|analysis/probes|RIPR-SPEC-0112: bounded subprocess adapter recognizer and fixture corpus inspect an allowlisted literal command; this source does not execute networking or add a network client.
crates/ripr/src/analysis/syntax/ra.rs|"push"|1|analysis/rust-syntax|Rust syntax heuristics classify a method named push as a generic mutation effect; this string does not invoke Git or network access.
crates/ripr/src/lsp/backend.rs|"push"|1|lsp/diagnostics|Push/pull diagnostic parity classifies the transport name as data; this literal does not execute Git or networking.
crates/ripr/src/lsp/tests.rs|"push"|1|lsp/tests|Push/pull parity fixture names the push transport while comparing diagnostic actions; it performs no network operation.
crates/ripr/src/output/perl_gap_record_projection.rs|curl|5|analysis-perl|Absence-assertions mirroring the TS projection: comments + unit tests verifying the Perl receipt_command is a fixed `ripr outcome` shape that never invokes curl/an external provider. No network behavior; these reference the term only to prove it is absent.
crates/ripr/src/output/pr_inline_comment_publish_plan.rs|"push"|1|output/pr-inline-plan|A unit fixture names the GitHub push event to prove inline publication is blocked for an unsafe event; it performs no network operation.
crates/ripr/src/output/typescript_packet_projection.rs|curl|4|analysis-typescript|F7 absence-assertions: comments + unit tests verifying the TS receipt_command is a fixed `ripr outcome` shape that never invokes curl/an external provider. No network behavior; these reference the term only to prove it is absent.
editors/vscode/src/downloader.ts|https.get|1|lsp/editor|VS Code extension downloads verified server assets from the configured release source.
xtask/src/branch_inventory.rs|"push"|2|repo-ops/branch-inventory|Owner-confirmed stale-branch deletion constructs one force-with-lease Git push command and its exact unit-test expectation.
xtask/src/main.rs|"push"|1|xtask/test-efficiency|The test-efficiency analyzer ignores the common Vec push method when classifying meaningful test calls; this token does not invoke Git or network access.
xtask/src/reports/source_promotion_admission_fixture.rs|curl|8|release/source-promotion-admission|RIPR-SPEC-0150: the deterministic J5 fixture retains exact governed curl-token cardinality across synthetic source-only policy rows and tests; it executes the production checker locally and performs no network request.
xtask/src/reports/source_promotion_control/publication.rs|"push"|1|release/source-promotion|RIPR-SPEC-0150: guarded candidate-ref publication is the only production Git network mutation in the recovered controller and requires exact URL, expected-state lease, and post-attempt reconciliation.
xtask/src/reports/source_promotion_control/tests.rs|"push"|11|release/source-promotion-tests|RIPR-SPEC-0150: isolated local bare-repository fixtures seed, clear, and reseed source and W7 refs while exercising immutable-object publication under local-ref, up-to-date/no-op, malformed-success attribution, and attributed-update-then-divergent-remote races, fail-closed post-push observation, unknown-state rollback, and remote rejection without touching a production remote.
xtask/src/tests.rs|curl|2|ci/release-qualification|PR #3147 structured workflow security oracle requires the exact unauthenticated rulesets API marker and rejects replacing it with authenticated gh api; reviewed 2026-08-11.
xtask/tests/source_promotion_resolved_tree.rs|curl|28|release/source-promotion|J5 regression fixture intentionally embeds governed curl literals to prove source-only network ledgers reject and semantic reconciliation passes without executing networking.
xtask/tests/source_promotion_workflow_contract.rs|curl|4|release/source-promotion-admission|The authentic J5 workflow E2E embeds exact governed curl literals to construct a deterministic rejected policy receipt; it executes the local production checker and performs no network request.
