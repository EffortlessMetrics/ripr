# Handoff: 0.7.0 Release Execution Closeout

Date: 2026-05-20

Branch / PR: `release/0.7.0-execution-closeout` / pending

Latest merged proof PRs:

- `#1351` `release: prove 0.7.0 source artifacts`;
- `#1352` `release: record 0.7.0 ship decision`;
- `#1353` `release: record 0.7.0 crate publish proof`;
- `#1355` `release: record 0.7.0 server and VSIX proof`;
- `#1356` `release: record 0.7.0 editor publish proof`.

## Release Result

Result: RIPR 0.7.0 is shipped.

RIPR 0.7.0 shipped as the actionable static repair-loop release:

```text
existing evidence
-> actionable gap packet
-> ranked swarm plan
-> dry-run repair context
-> focused external edit
-> verify command
-> receipt command
-> actionable-gap outcome
-> next packet or no-action
```

The release was executed from source `EffortlessMetrics/ripr`. No publish or
signing secrets were moved into `ripr-swarm`.

## Published Surfaces

| Surface | Status | Proof |
| --- | --- | --- |
| crates.io crate | published as `ripr 0.7.0` | [0.7.0 Crate Publish Proof](2026-05-20-0.7.0-crate-publish-proof.md) |
| GitHub Release | published as `v0.7.0` | [0.7.0 Server And VSIX Artifact Proof](2026-05-20-0.7.0-server-vsix-proof.md) |
| Server assets | manifest, checksums, and five target archives uploaded | [0.7.0 Server And VSIX Artifact Proof](2026-05-20-0.7.0-server-vsix-proof.md) |
| GitHub Release VSIX | `ripr-0.7.0.vsix` attached | [0.7.0 Server And VSIX Artifact Proof](2026-05-20-0.7.0-server-vsix-proof.md) |
| VS Code Marketplace | `EffortlessMetrics.ripr 0.7.0` visible | [0.7.0 Editor Marketplace Publish Proof](2026-05-20-0.7.0-editor-publish-proof.md) |
| Open VSX | `EffortlessMetrics.ripr 0.7.0` visible | [0.7.0 Editor Marketplace Publish Proof](2026-05-20-0.7.0-editor-publish-proof.md) |

## Source And Tag State

Source `main` after editor publish proof:

```text
214af2573686820fd41a0b3559ae0efaf166df1f
```

Release tag:

```text
ea629a95d8cc97de4c5c76a4d77b8ca64163703e refs/tags/v0.7.0
```

The `v0.7.0` tag points at the release artifact base after crate publication
proof. Later source PRs `#1355`, `#1356`, and this closeout are post-tag audit
and proof records; they do not alter the published crate, server archive, or
VSIX contents.

GitHub Release:

```text
tag: v0.7.0
name: ripr 0.7.0
draft: false
prerelease: false
published: 2026-05-20T23:45:59Z
url: https://github.com/EffortlessMetrics/ripr/releases/tag/v0.7.0
```

The GitHub Release body was updated with the 0.7.0 release notes and explicit
non-claims after marketplace publication completed.

## Artifact Inventory

GitHub Release assets:

- `checksums.txt`;
- `ripr-0.7.0.vsix`;
- `ripr-server-manifest-v0.7.0.json`;
- `ripr-server-v0.7.0-aarch64-apple-darwin.tar.gz`;
- `ripr-server-v0.7.0-aarch64-apple-darwin.tar.gz.sha256`;
- `ripr-server-v0.7.0-aarch64-unknown-linux-gnu.tar.gz`;
- `ripr-server-v0.7.0-aarch64-unknown-linux-gnu.tar.gz.sha256`;
- `ripr-server-v0.7.0-x86_64-apple-darwin.tar.gz`;
- `ripr-server-v0.7.0-x86_64-apple-darwin.tar.gz.sha256`;
- `ripr-server-v0.7.0-x86_64-pc-windows-msvc.zip`;
- `ripr-server-v0.7.0-x86_64-pc-windows-msvc.zip.sha256`;
- `ripr-server-v0.7.0-x86_64-unknown-linux-gnu.tar.gz`;
- `ripr-server-v0.7.0-x86_64-unknown-linux-gnu.tar.gz.sha256`.

Server manifest target set:

```text
aarch64-apple-darwin
aarch64-unknown-linux-gnu
x86_64-apple-darwin
x86_64-pc-windows-msvc
x86_64-unknown-linux-gnu
```

Windows server smoke:

```text
ripr 0.7.0
ripr-lsp 0.7.0
```

Windows archive checksum:

```text
1aa0496c265f46c3f38a720f2482149650ad0e09c251307d31b923085e5977a6
```

The checksum matched the release manifest.

## Registry Verification

crates.io:

```bash
cargo search ripr --limit 5
```

Observed:

```text
ripr = "0.7.0"
```

VS Code Marketplace:

```bash
npm exec --yes -- @vscode/vsce show EffortlessMetrics.ripr --json
```

Observed latest version:

```text
0.7.0
```

Open VSX:

```bash
npm exec --yes -- ovsx get EffortlessMetrics.ripr --metadata
```

Observed version:

```text
0.7.0
```

Observed Open VSX download URL:

```text
https://open-vsx.org/api/EffortlessMetrics/ripr/0.7.0/file/EffortlessMetrics.ripr-0.7.0.vsix
```

## Accepted Warnings And Limitations

- The full current `ripr-swarm` live audit timed out before selecting a live
  current-repo repair packet. This remains an accepted 0.7 limitation, not a
  hidden success.
- `repo-exposure-latency` remains warning-only in the 0.7.0 release proof.
- The editor package workflow emitted the existing VSIX file-count advisory
  for 332 files and 173 JavaScript files.
- `npm ci` reported existing dependency-audit findings during source proof.
- Static VS Marketplace install-count badges were not refreshed during this
  release. Any badge refresh must be a separate scoped badge PR using publisher
  metrics or an explicit deferred-badge record.

## Non-Claims

0.7.0 does not claim:

- autonomous repair;
- source-edit automation;
- generated tests;
- provider or model calls;
- runtime mutation execution;
- runtime adequacy;
- coverage adequacy;
- correctness proof;
- default CI blocking;
- public badge semantic changes;
- full current `ripr-swarm` live audit completion;
- live current-repo repair selection.

## Validation Trail

Source release proof:

```text
cargo fmt --check
cargo check --workspace --all-targets
cargo test --workspace --all-targets
cargo clippy --workspace --all-targets -- -D warnings
cargo doc --workspace --no-deps
cargo xtask check-pr
cargo xtask check-output-contracts
cargo xtask check-traceability
cargo xtask check-capabilities
cargo xtask check-fixture-contracts
cargo xtask lsp-cockpit-report
cargo xtask release-readiness --version 0.7.0
cargo xtask check-product-copy
cargo xtask check-generated-clean
cargo package -p ripr --list
cargo publish -p ripr --dry-run
npm --prefix editors/vscode ci
npm --prefix editors/vscode run compile
npm --prefix editors/vscode run package
cargo run -p ripr -- --version
cargo run -p ripr -- first-pr --help
git diff --check
```

Publish and distribution proof:

```text
cargo publish -p ripr
cargo install ripr --version 0.7.0 --locked --root target/ripr/install-smoke-cratesio-0.7.0 --force
target/ripr/install-smoke-cratesio-0.7.0/bin/ripr.exe --version
target/ripr/install-smoke-cratesio-0.7.0/bin/ripr.exe first-pr --help
gh workflow run release-server-binaries.yml -f version=0.7.0
gh run watch 26196081731 --interval 30 --exit-status
gh release download v0.7.0 --repo EffortlessMetrics/ripr --pattern ...
target/ripr/release-smoke-0.7.0/windows/ripr.exe --version
target/ripr/release-smoke-0.7.0/windows/ripr.exe lsp --version
certutil -hashfile target\ripr\release-smoke-0.7.0\ripr-server-v0.7.0-x86_64-pc-windows-msvc.zip SHA256
gh workflow run publish-extension.yml --field version=0.7.0 --field publish_vs_marketplace=true --field publish_open_vsx=true
gh run watch 26197301988 --interval 30 --exit-status
npm exec --yes -- @vscode/vsce show EffortlessMetrics.ripr --json
npm exec --yes -- ovsx get EffortlessMetrics.ripr --metadata
```

Closeout PR validation:

```text
cargo xtask check-doc-index
cargo xtask markdown-links
cargo xtask check-static-language
cargo xtask check-product-copy
cargo xtask check-pr
git diff --check
```

## Follow-Up Work

- Marketplace badge refresh remains separate and must use publisher metrics or
  record an explicit static-badge deferral.
- `ripr-swarm` should continue normal development trunk work for the next train.
- Source `ripr` remains release, promotion, security, and distribution
  authority.
- Do not rerun 0.7 publish workflows unless recovering a named missing or
  corrupt surface.

## What Not To Do

- Do not publish 0.7.0 again unless recovering a documented failed surface.
- Do not move release, publish, or signing secrets into `ripr-swarm`.
- Do not retag `v0.7.0`.
- Do not treat post-tag proof PRs as release artifact content changes.
- Do not refresh marketplace badges inside unrelated release closeout work.
- Do not hide the accepted repo-exposure latency warning or full
  `ripr-swarm` live-audit timeout limitation.
