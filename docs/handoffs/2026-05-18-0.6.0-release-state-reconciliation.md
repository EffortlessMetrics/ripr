# Handoff: 0.6.0 Release State Reconciliation

Date: 2026-05-18
Branch / PR: `release/0.6x-post-release-reconciliation` / TBD
Tagged release base: `fd4d9cb1b65c3f17a1a931bebaabc5fda4d6af74`
(`#1218` `docs(release): clarify publish decision base`)
Current reconciliation base: `0e2a34d95186815f4893eff6d84a7027b43d6b06`
(`#1220` `docs(release): align 0.6.0 changelog`)

## Current Work Item

`release: reconcile post-execution 0.6.0 state`

This handoff records the live release state after the release was executed
outside the release-prep PRs. It does not tag, publish, create or edit a GitHub
Release, upload assets, publish crates.io or marketplace packages, refresh
badge endpoints, change branch protection, change CI blocking, promote preview
language evidence, run providers, run mutation tests, generate tests, edit
source files, adopt baselines, or create suppressions.

## Release State

Status: `v0.6.0` has been cut.

Verified live state:

- GitHub Release: `v0.6.0`
- Tag target: `fd4d9cb1b65c3f17a1a931bebaabc5fda4d6af74`
- GitHub Release published: 2026-05-18T07:50:45Z
- crates.io: `ripr = "0.6.0"`
- GitHub Release assets include:
  - `ripr-0.6.0.vsix`
  - `ripr-server-manifest-v0.6.0.json`
  - `checksums.txt`
  - per-target server archives and `.sha256` files for Linux, macOS, and
    Windows targets

The release execution was not performed by the publish-decision PR. The earlier
publish-decision runbook is now historical; do not run it again unless a
maintainer explicitly opens a release recovery task.

## Post-Tag Main Changes

The tag points at `fd4d9cb`. `main` has moved after the tag:

- `#1217` refreshed generated public badge endpoint JSON and removed a stale
  test-intent declaration.
- `#1220` moved the accepted #1210 changelog note into the 0.6.0 section and
  left `Unreleased` empty.

These are release-state housekeeping changes after the tag. They do not create
new 0.6.0 product claims and must not be used as a reason to retag or republish
without an explicit maintainer release-recovery decision.

## Open PR State

At reconciliation time, the only open Codex PR was:

- `#1219` `xtask: bound PR evidence generation`

Disposition: release-readiness-positive but post-tag. It should land if remote
checks pass because it bounds PR evidence generation and writes a contract-valid
`status = "error"` packet on timeout or unusable analyzer output. It is not
part of the published `v0.6.0` tag and should be treated as post-release
hardening unless a maintainer explicitly opens a retag or patch-release task.

## Verification Run

Live release state:

```bash
gh release view v0.6.0 --json tagName,isDraft,isPrerelease,publishedAt,targetCommitish,assets,url
cargo search ripr --limit 5
git ls-remote --tags origin v0.6.0
```

Observed:

- GitHub Release exists, is not draft, and is not prerelease.
- crates.io reports `ripr = "0.6.0"`.
- remote tag `v0.6.0` resolves to
  `fd4d9cb1b65c3f17a1a931bebaabc5fda4d6af74`.

Post-#1217 local proof already recorded before this reconciliation:

```bash
cargo xtask check-badge-endpoints
cargo xtask check-generated-clean
cargo xtask check-product-copy
cargo xtask release-readiness --version 0.6.0
cargo xtask check-pr
git diff --check
```

Observed:

- badge endpoints were refreshed and checked in #1217;
- `cargo xtask release-readiness --version 0.6.0` passed all required checks
  and retained only the accepted non-required `repo-exposure-latency` warning;
- `cargo xtask check-pr` passed.

## Current Boundaries

Still true after release execution:

- Rust is the stable evidence path.
- TypeScript/JavaScript/Python remain opt-in preview/advisory evidence.
- Static evidence remains advisory by default.
- RIPR does not claim runtime mutation proof, coverage-completeness proof,
  generated tests, provider execution, source edits, default CI blocking,
  baseline adoption, suppression creation, or preview-language gate authority.

## Recommended Next Action

Finish any already-open post-tag hardening PRs that are narrow and green,
starting with #1219 if its remote checks pass. Then record a separate
post-release patch or follow-up handoff if maintainers want to ship those
changes. Do not move the `v0.6.0` tag.

## What Not To Do

- Do not rerun the 0.6.0 release execution steps.
- Do not retag `v0.6.0`.
- Do not republish crates.io or marketplace packages for `0.6.0`.
- Do not treat post-tag `main` changes as part of the already published
  `v0.6.0` artifact.
- Do not silently promote preview-language support or gate authority.
