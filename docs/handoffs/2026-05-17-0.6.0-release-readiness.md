# Handoff: 0.6.0 Release Readiness

Date: 2026-05-17
Branch / PR: `release-0.6.0-readiness` / #1095
Latest merged PR: #1095 `docs(release): add 0.6.0 readiness handoff`
(commit `28923b00a12ec8549ee670a131a5031ecc2c6311`)

## Current Work Item

`docs(release): add 0.6.0 release readiness handoff`

0.6.0 is a feature and experience release, not the earlier planned 0.5.1
quality patch. The release line now includes evidence-trust improvements,
policy-operation packets, preview-language visibility, editor first-run
polish, generated-CI and repo-operations front doors, and release-copy
guardrails.

The release remains static and advisory by default:

```text
diff or workspace evidence
-> canonical static evidence
-> editor, CI, policy, and repo-ops packets
-> maintainer judgment or explicit configured gate
```

It must not be described as mutation execution, generated tests, runtime proof,
default blocking, automatic policy mutation, or full multi-language authority.

## What Changed Since 0.5.0

| Theme | Release Meaning |
| --- | --- |
| Evidence trust | Raw findings roll up into canonical evidence items; finding-to-gap alignment, actionability, repair routes, and named static limitations make review output easier to govern. |
| Operator trust | Policy operations, history, promotion packets, preview-promotion packets, generated-evidence discipline, command mutability, PR-ready, repo cockpit, merge-watch policy, PR triage dispositions, receipts, critic, and suggested fixes make agentic PR work safer. |
| Editor trust | First-run setup diagnosis, Show Status context, receipt state, first repair packet, first-run usability fixtures, first-run-to-first-receipt docs, and editor repair dogfood receipts make the editor path more self-orienting. |
| CI trust | Generated-CI policy packets, first-run summaries, report indexing, release-copy checks, and advisory artifact projection make CI evidence easier to read without adding default blocking. |
| Preview-language honesty | TypeScript, JavaScript, and Python preview adapters are visible and useful, but remain syntax-first, advisory, static-limit bounded, and non-gating by default. |
| Release identity | #1094 aligned the crate, lockfile, VS Code package metadata, changelog, release docs, marketplace docs, server provisioning examples, schema examples, and receipt fixtures to 0.6.0. |

Representative closeout and final hardening PRs:

- #924 closed Policy Operations and Promotion Readiness.
- #1056 closed the repo-operations UX cockpit lane.
- #1057 required named static limitations.
- #1051 and #1052 added focused output hardening coverage.
- #1071 closed First-Run UX and Adoption Hardening.
- #1094 prepared the 0.6.0 release identity.

The changelog is the complete release narrative. This handoff is the release
operator boundary.

## Stable, Advisory, And Preview Surfaces

Stable for 0.6.0:

- Rust static mutation-exposure evidence remains the stable product path.
- CLI, JSON, Markdown, editor, generated-CI, and repo-ops packets remain
  source-edit-free and artifact-first.
- `cargo xtask check-product-copy`, `check-pr`, `check-generated-clean`,
  `check-static-language`, and release-readiness checks are release proof
  surfaces.

Advisory by default:

- RIPR findings, generated-CI summaries, policy operations, policy history,
  promotion packets, preview-promotion packets, repo cockpit, PR-ready,
  PR triage, critic, and suggested fixes.
- Calibrated or blocking behavior requires explicit configured policy and the
  already-documented promotion path.

Preview:

- TypeScript, JavaScript, and Python evidence.
- Preview evidence is visible and useful, but not RIPR Zero blocking debt,
  default gate material, mutation-calibrated confidence, or runtime proof.

## Public Claims To Make

Use product-facing wording like:

```text
ripr 0.6.0 makes static test-gap review easier to trust and easier to operate:
better evidence alignment, clearer editor and CI guidance, preview language
visibility, and repo-local cockpit tools for safe agentic PR work.
```

Safe public claims:

- Static mutation-exposure analysis helps identify weak test signals before
  expensive mutation testing.
- Rust is the stable evidence path.
- TypeScript, JavaScript, and Python are preview evidence paths.
- Editor, CI, and agent surfaces make advisory evidence easier to act on.
- Repo-ops tools make high-volume PR work safer by separating authored truth,
  generated evidence, deterministic repair, and judgment-required decisions.

## Public Claims Not To Make

- Do not claim runtime mutation testing, killed/survived outcomes, test
  adequacy, or proof.
- Do not claim generated tests, provider calls, source edits, or editor patches.
- Do not claim TypeScript, JavaScript, or Python evidence is stable gate
  authority.
- Do not claim default CI blocking, branch-protection changes, automatic
  baseline adoption, suppression creation, config mutation, or preview-language
  promotion.
- Do not describe internal lane names, canonical gaps, evidence spine, front
  panel, or cockpit as the first-screen release value.

## Proof Completed Before This Handoff

#1094 completed the release-shaping proof:

```bash
cargo xtask check-product-copy
cargo xtask check-doc-index
cargo xtask markdown-links
cargo xtask check-static-language
cargo test -p ripr agent_receipt
cargo test -p ripr --test cli_smoke editor_agent_loop_fixture_outputs_match_expected
npm --prefix editors/vscode ci
npm --prefix editors/vscode run compile
cargo xtask check-generated-clean
git diff --check
cargo xtask check-pr
```

The handoff PR should remain docs-only and rerun:

```bash
cargo xtask check-doc-index
cargo xtask markdown-links
cargo xtask check-static-language
cargo xtask check-generated-clean
cargo xtask check-pr
git diff --check
```

## Release Proof Still Required

Do not tag or publish until the release proof PR records these commands:

```bash
cargo fmt --check
cargo check --workspace --all-targets
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo doc --workspace --no-deps
cargo xtask check-product-copy
cargo xtask release-readiness --version 0.6.0
cargo package -p ripr --list
cargo publish -p ripr --dry-run
npm --prefix editors/vscode ci
npm --prefix editors/vscode run compile
npm --prefix editors/vscode run package
```

Also run the smoke commands named in `docs/RELEASE.md`, including GitHub
Release asset verification after `v0.6.0` exists.

## Artifact Requirements Before Publish

- `crates/ripr/Cargo.toml` and `Cargo.lock` package version are 0.6.0.
- `editors/vscode/package.json` and `package-lock.json` are 0.6.0.
- VSIX filename is `ripr-0.6.0.vsix`.
- GitHub Release tag and server asset names use `v0.6.0`.
- Server manifest is `ripr-server-manifest-v0.6.0.json`.
- Server archives, checksums, and manifest checksums match.
- `cargo publish -p ripr --dry-run` passes.
- Extension compile and package pass.
- Release copy checklist passes.
- Generated badge endpoints are not manually edited; badge refresh remains a
  separate generated PR.

## Current Board Boundary

At handoff authoring, #1057, #1052, and #1051 are already merged. They are not
open release blockers.

Open non-release PRs may continue after 0.6.0, but do not absorb broad new
feature work into the release unless a release proof command exposes a truth
issue. The release path should now be:

```text
0.6.0 release-readiness handoff
-> 0.6.0 dry-run proof
-> tag / release assets
-> crates.io and marketplace publish
-> post-publish verification
```

## What Not To Do

- Do not open another broad product lane before 0.6.0.
- Do not refresh badge numbers in ordinary release docs PRs.
- Do not change analyzer semantics, policy authority, generated tests,
  provider behavior, mutation execution, default blocking, baselines,
  suppressions, branch protection, or preview-language authority.
- Do not present static evidence as runtime proof.
- Do not publish, tag, or create GitHub Releases without explicit release-proof
  evidence.
