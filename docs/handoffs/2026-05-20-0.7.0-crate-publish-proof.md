# Handoff: 0.7.0 Crate Publish Proof

Date: 2026-05-20

Branch / PR: `release/0.7.0-crate-publish-proof` / pending

Publish base: `345077ef254d0e99a2be0933009e8ef4aa156ea5`
(`#1352` `release: record 0.7.0 ship decision`)

## Current Work Item

`release: publish ripr 0.7.0 crate`

This handoff records the crates.io publication proof for `ripr` 0.7.0 from
source `EffortlessMetrics/ripr`.

It does not create a Git tag, create a GitHub Release, upload server assets,
publish VS Code Marketplace or Open VSX artifacts, refresh badge endpoints,
change branch protection, change default CI blocking, run providers, generate
tests, edit user source, or run mutation testing.

## Publish Result

Result: crate published.

`cargo publish -p ripr` completed successfully and Cargo reported:

```text
Uploaded ripr v0.7.0 to registry `crates-io`
Published ripr v0.7.0 at registry `crates-io`
```

Registry visibility was verified after publish:

```text
cargo search ripr --limit 5
-> ripr = "0.7.0"
```

`cargo info ripr` reported:

```text
version: 0.7.0
license: MIT OR Apache-2.0
rust-version: 1.95
repository: https://github.com/EffortlessMetrics/ripr/
```

## Pre-Publish Gates

The final no-drift gates from the ship-decision packet passed on source
`main` before publish:

```bash
cargo xtask check-generated-clean
cargo xtask release-readiness --version 0.7.0
cargo publish -p ripr --dry-run
npm --prefix editors/vscode run package
git diff --check
```

`cargo xtask release-readiness --version 0.7.0` still reported overall
`warn` because `repo-exposure-latency` is advisory and not passing. All
required release-readiness checks passed.

`cargo publish -p ripr --dry-run` passed immediately before publication:

```text
Packaged 199 files, 4.6MiB (775.9KiB compressed)
warning: aborting upload due to dry run
```

`npm --prefix editors/vscode run package` passed and produced:

```text
editors/vscode/dist/ripr-0.7.0.vsix
332 files, 527.72 KB
```

The existing VSIX JavaScript file-count warning remains accepted from the
ship-decision packet.

## Post-Publish Smoke

Installed crate smoke passed:

```bash
cargo install ripr --version 0.7.0 --locked --root target/ripr/install-smoke-cratesio-0.7.0 --force
target/ripr/install-smoke-cratesio-0.7.0/bin/ripr.exe --version
target/ripr/install-smoke-cratesio-0.7.0/bin/ripr.exe first-pr --help
```

Observed output:

```text
ripr 0.7.0
Create the start-here packet for one PR from existing RIPR artifacts.
```

## Source State

- source repo: `EffortlessMetrics/ripr`;
- publish base: `345077ef254d0e99a2be0933009e8ef4aa156ea5`;
- crate version in source: `0.7.0`;
- VS Code extension version in source: `0.7.0`;
- source worktree after publish: clean.

No source diff was required for the publish operation itself. This handoff is
the only source change in this PR.

## Boundaries Preserved

The published crate carries the 0.7.0 actionable static repair-loop claim:

```text
existing evidence
-> actionable gap packet
-> ranked swarm plan
-> dry-run repair context
-> focused human or agent edit outside RIPR
-> verify command
-> receipt command
-> actionable-gap outcome
-> next packet or no-action
```

This publish proof does not claim:

- autonomous repair;
- source-edit automation;
- generated tests;
- provider or model calls;
- runtime mutation execution;
- killed or survived mutation outcomes;
- runtime adequacy;
- coverage adequacy;
- correctness proof;
- default CI blocking;
- gate eligibility by default;
- TypeScript, JavaScript, or Python preview promotion;
- current full `ripr-swarm` live audit completion or live current-repo repair
  selection.

## Next Required Release Work

The next scoped release PR should be:

```text
release: package 0.7.0 server and VSIX artifacts
```

That PR should record server archive, manifest/checksums, VSIX package, install
verification, version verification, and GitHub Release asset status if release
assets are produced by the normal workflow.

Editor marketplace publication remains a separate explicit decision after
server/VSIX artifact proof.

## What Not To Do

- Do not rerun `cargo publish -p ripr` for 0.7.0.
- Do not publish from `ripr-swarm`.
- Do not tag, create a GitHub Release, upload server assets, or publish editor
  marketplace artifacts from this crate-publish proof.
- Do not hide the accepted repo-exposure latency warning or full
  `ripr-swarm` live-audit timeout limitation.
