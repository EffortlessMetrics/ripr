name: One-use #1574 exact transformer

on:
  pull_request:
    types: [synchronize]

permissions: {}

jobs:
  apply:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'staging exact chunked transformer; execution intentionally disabled'
