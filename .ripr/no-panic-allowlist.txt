# Existing panic-family debt and intentional string-detection cases.
# Format: path|pattern|max_count|reason
#
# New production or test code should not add entries here unless the exception
# is reviewed. The preferred direction is to remove entries.

crates/ripr/src/analysis/mod.rs|unwrap(|10|existing test debt tracked in docs/METRICS.md
crates/ripr/src/analysis/rust_index.rs|expect(|2|string pattern detects analyzed user code, not a call site
crates/ripr/src/analysis/rust_index.rs|unwrap(|2|string pattern detects analyzed user code, not a call site
crates/ripr/src/lsp.rs|expect(|1|existing production debt tracked in docs/METRICS.md
crates/ripr/src/lsp.rs|unwrap(|1|existing test debt tracked in docs/METRICS.md
crates/ripr/tests/cli_smoke.rs|unwrap(|2|existing test debt tracked in docs/METRICS.md
xtask/src/main.rs|expect(|1|test harness setup for parsing test intent kinds
xtask/src/main.rs|panic!|1|test assertion for unreachable status value
xtask/src/main.rs|unwrap(|12|test harness functions (temp_dir, write, with_temp_cwd) managing test isolation
