id = "agentic-devex-foundation"
title = "Make ripr ready for Codex Goals implementation"
status = "active"

objective = """
Build the repo operating system needed for Codex Goals and human contributors to
work through ripr's implementation plan safely.
"""

end_state = [
  "architecture guard exists",
  "output-contract checks exist",
  "first behavior fixtures exist",
  "docs-as-tests baseline exists",
  "test-oracle report exists",
  "dogfood report exists",
  "Codex Goals campaign docs exist"
]

[[work_item]]
id = "policy/architecture-guard"
status = "done"
branch = "policy/architecture-guard"
stackable = false
requires_human_merge = true
acceptance = "workspace-shape, architecture, and public API checks pass"
commands = [
  "cargo xtask check-workspace-shape",
  "cargo xtask check-architecture",
  "cargo xtask check-public-api",
  "cargo xtask check-pr"
]

[[work_item]]
id = "output/output-contract-check"
status = "done"
branch = "policy/output-contract-check"
stackable = false
requires_human_merge = true
acceptance = "cargo xtask check-output-contracts validates current output contract registries"
commands = [
  "cargo xtask check-output-contracts",
  "cargo xtask check-pr"
]

[[work_item]]
id = "docs/docs-index-checks"
status = "done"
branch = "docs/doc-index-checks"
stackable = true
requires_human_merge = true
acceptance = "docs index checks validate README, docs map, spec index, and ADR index"
commands = [
  "cargo xtask check-doc-index",
  "cargo xtask check-pr"
]

[[work_item]]
id = "docs/codex-goals-campaigns"
status = "done"
branch = "docs/codex-goals-campaigns"
stackable = true
requires_human_merge = true
acceptance = "Codex Goals docs describe multi-PR campaign mode and PR-sized work item contracts"
commands = [
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask check-traceability",
  "cargo xtask check-capabilities"
]

[[work_item]]
id = "fixtures/first-two-goldens"
status = "ready"
branch = "fixtures/first-two-goldens"
stackable = false
requires_human_merge = true
acceptance = "boundary_gap and weak_error_oracle fixtures exist and pass fixture/golden checks"
commands = [
  "cargo xtask fixtures",
  "cargo xtask goldens check",
  "cargo xtask check-pr"
]

[[work_item]]
id = "testing/test-oracle-report"
status = "ready"
branch = "testing/test-oracle-report"
stackable = false
requires_human_merge = true
acceptance = "ripr's own test oracle strength report is generated without changing analyzer behavior"
commands = [
  "cargo xtask check-pr"
]

[[work_item]]
id = "dogfood/static-self-check"
status = "ready"
branch = "dogfood/static-self-check"
stackable = false
requires_human_merge = true
acceptance = "focused ripr-on-ripr report is generated as a non-blocking artifact"
commands = [
  "cargo xtask check-pr"
]
