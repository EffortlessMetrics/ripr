id = "hot-sidecar-latency"
title = "Hot Sidecar Latency Proof"
status = "active"

# This is Campaign 9 in `docs/IMPLEMENTATION_CAMPAIGNS.md`.
# Campaign 8 (`runtime-calibration-fixtures`) closed after the supplied
# cargo-mutants fixture set pinned the main static/runtime agreement buckets.
# This lane is performance and editor responsiveness, not new analyzer
# semantics or new LSP product surface.

objective = """
Make the editor and operator paths faster without broadening the analyzer or LSP
surface by measuring current cache and refresh behavior first, then tightening
warm-path reuse only where there is evidence.
"""

end_state = [
  "Current repo seam cache behavior and saved-workspace LSP refresh latency are measured from existing commands",
  "Any hot-path cache change preserves output schemas, static vocabulary, public API, SARIF, badges, and saved-workspace LSP cockpit behavior",
  "Rendered outputs remain uncached; only fact layers or in-memory indexes may be reused",
  "Large-repo and editor latency decisions are backed by reports, not speculative storage"
]

[[work_item]]
id = "cache/current-latency-audit"
status = "done"
branch = "hot-sidecar-latency-audit"
stackable = false
requires_human_merge = true
acceptance = "Current repo seam cache behavior, operator report generation, and saved-workspace LSP refresh proof are measured without changing analyzer, output, LSP, SARIF, badge, or public API behavior"
commands = [
  "cargo test -p ripr analysis::seam_cache --lib",
  "cargo test -p ripr analysis::seam_inventory --lib",
  "cargo test -p ripr lsp",
  "cargo test -p ripr lsp::tests",
  "cargo xtask lsp-cockpit-report",
  "cargo xtask operator-cockpit",
  "cargo xtask check-output-contracts",
  "cargo xtask check-static-language",
  "cargo xtask check-campaign",
  "cargo xtask goals next",
  "cargo xtask check-pr",
  "cargo test --workspace"
]

[[work_item]]
id = "cache/repo-exposure-latency-report"
status = "done"
branch = "repo-exposure-latency-report"
stackable = false
requires_human_merge = true
acceptance = "Add bounded repo-exposure latency instrumentation or reporting so cache hit/miss and phase timing can be observed before optimizing warm-path reuse; do not change analyzer outputs"
commands = [
  "cargo test -p ripr analysis::seam_cache --lib",
  "cargo test -p ripr analysis::seam_inventory --lib",
  "cargo test -p ripr lsp",
  "cargo test -p ripr lsp::tests",
  "cargo xtask lsp-cockpit-report",
  "cargo xtask repo-exposure-latency-report",
  "cargo xtask check-output-contracts",
  "cargo xtask check-static-language",
  "cargo xtask check-campaign",
  "cargo xtask goals next",
  "cargo xtask check-pr",
  "cargo test --workspace"
]

[[work_item]]
id = "cache/repo-exposure-warm-path-reuse"
status = "done"
branch = "repo-exposure-warm-path-reuse"
stackable = false
requires_human_merge = true
acceptance = "Use repo-exposure latency evidence to reduce warm-path recomputation without caching rendered JSON, Markdown, diagnostics, hovers, SARIF, badges, or agent packets; preserve analyzer outputs and public contracts"
commands = [
  "cargo xtask repo-exposure-latency-report",
  "cargo test -p ripr analysis::seam_cache --lib",
  "cargo test -p ripr analysis::seam_inventory --lib",
  "cargo test -p ripr lsp",
  "cargo test -p ripr lsp::tests",
  "cargo xtask lsp-cockpit-report",
  "cargo xtask check-output-contracts",
  "cargo xtask check-static-language",
  "cargo xtask check-campaign",
  "cargo xtask goals next",
  "cargo xtask check-pr",
  "cargo test --workspace"
]

[[work_item]]
id = "pilot/budget-aware"
status = "done"
branch = "pilot-budget-aware"
stackable = false
requires_human_merge = true
acceptance = "Make ripr pilot bounded and explicit under timeout or partial analysis so the first-run operator path does not hang silently; preserve analyzer outputs and public schemas unless intentionally versioned"
commands = [
  "cargo test -p ripr cli::commands::tests::pilot",
  "cargo test -p ripr --test cli_smoke pilot_",
  "cargo xtask repo-exposure-latency-report",
  "cargo xtask check-output-contracts",
  "cargo xtask check-static-language",
  "cargo xtask check-campaign",
  "cargo xtask goals next",
  "cargo xtask check-pr",
  "cargo test --workspace"
]

[[work_item]]
id = "pilot/first-screen-clarity"
status = "done"
branch = "pilot-first-screen-clarity"
stackable = false
requires_human_merge = true
acceptance = "Improve pilot-summary.md and terminal copy so the top recommendation answers what was inspected, why the seam matters, what focused test to write, and what command to run after without opening JSON"
commands = [
  "cargo test -p ripr output::pilot --lib",
  "cargo test -p ripr cli::commands::tests::pilot",
  "cargo test -p ripr --test cli_smoke pilot_",
  "cargo xtask check-output-contracts",
  "cargo xtask check-static-language",
  "cargo xtask check-campaign",
  "cargo xtask goals next",
  "cargo xtask check-pr",
  "cargo test --workspace"
]

[[work_item]]
id = "campaign/hot-sidecar-latency-closeout"
status = "ready"
branch = "campaign-hot-sidecar-latency-closeout"
stackable = false
requires_human_merge = true
acceptance = "Close Campaign 9 after cache measurement, warm-path reuse, bounded pilot behavior, and first-screen pilot clarity have landed; update docs and manifests only unless closeout proof finds drift"
commands = [
  "cargo test -p ripr lsp",
  "cargo test -p ripr lsp::tests",
  "cargo xtask lsp-cockpit-report",
  "cargo xtask repo-exposure-latency-report",
  "cargo xtask check-output-contracts",
  "cargo xtask check-static-language",
  "cargo xtask check-campaign",
  "cargo xtask goals next",
  "cargo xtask check-pr",
  "cargo test --workspace"
]
