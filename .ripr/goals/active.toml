id = "evidence-quality"
title = "Make ripr findings evidence-first"
status = "active"

objective = """
Make findings explain changed behavior, oracle strength, propagation,
activation, and unknown stop reasons with enough precision to guide test work.
"""

end_state = [
  "oracle kind and strength are probe-relative",
  "local delta flow can name visible sinks",
  "activation modeling can name observed and missing discriminator values",
  "output is evidence-first",
  "unknown findings include stop reasons across surfaces"
]

[[work_item]]
id = "output/unknown-stop-reason-invariant"
status = "done"
branch = "output/unknown-stop-reason-invariant"
stackable = false
requires_human_merge = true
acceptance = "unknown findings include at least one stop reason across JSON and human output"
commands = [
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask fixtures",
  "cargo xtask goldens check",
  "cargo xtask check-output-contracts"
]

[[work_item]]
id = "analysis/oracle-strength-v2"
status = "done"
branch = "analysis/oracle-strength-v2"
stackable = false
requires_human_merge = true
acceptance = "oracle strength distinguishes exact discriminators from broad or smoke checks"
commands = [
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask fixtures",
  "cargo xtask goldens check",
  "cargo xtask check-output-contracts",
  "cargo xtask test-oracle-report"
]

[[work_item]]
id = "analysis/local-delta-flow-v1"
status = "ready"
branch = "analysis/local-delta-flow-v1"
stackable = false
requires_human_merge = true
acceptance = "changed expressions map to visible return, error, field, or effect sinks"
commands = [
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask fixtures",
  "cargo xtask goldens check",
  "cargo xtask golden-drift",
  "cargo xtask check-output-contracts"
]

[[work_item]]
id = "analysis/activation-value-modeling-v1"
status = "blocked"
branch = "analysis/activation-value-modeling-v1"
stackable = false
requires_human_merge = true
acceptance = "observed and missing discriminator values are tied to local flow evidence"
blocked_by = ["analysis/local-delta-flow-v1"]
blocked_reason = "requires local flow before observed and missing values can be tied to changed behavior"

[[work_item]]
id = "output/evidence-first-output"
status = "blocked"
branch = "output/evidence-first-output"
stackable = false
requires_human_merge = true
acceptance = "human and JSON output explain changed behavior, evidence path, weakness, and next action"
blocked_by = [
  "analysis/oracle-strength-v2",
  "analysis/local-delta-flow-v1",
  "analysis/activation-value-modeling-v1"
]
blocked_reason = "requires stable oracle, flow, and activation evidence fields before output reshaping"

[[work_item]]
id = "fixtures/negative-metamorphic-baseline"
status = "blocked"
branch = "fixtures/negative-metamorphic-baseline"
stackable = false
requires_human_merge = true
acceptance = "negative and metamorphic fixtures protect evidence-first output from noise and syntax brittleness"
blocked_by = ["output/evidence-first-output"]
blocked_reason = "requires evidence-first output expectations before expanding negative and metamorphic fixtures"
