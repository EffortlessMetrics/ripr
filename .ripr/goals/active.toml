id = "test-efficiency"
title = "Make low-discriminator tests visible"
status = "active"

objective = """
Make ripr report advisory test-efficiency and likely-vacuity signals from the
same evidence facts used for static exposure findings.
"""

end_state = [
  "per-test evidence ledgers name owners, oracle kind and strength, observed values, and missing discriminator overlap",
  "likely vacuous, smoke-only, broad-oracle, opaque, circular, and duplicative signals are advisory",
  "reports explain evidence and suggested next steps without calling tests bad",
  "test-efficiency metrics are available for trend tracking",
  "agent and editor surfaces can avoid imitating low-discriminator tests",
  "ripr and ripr+ badge artifacts publish unresolved-finding counts as inbox-zero signals",
  "test intent and suppressions are durable, reasoned exception files rather than allowlist entries"
]

[[work_item]]
id = "test-efficiency/test-fact-ledger"
status = "done"
branch = "test-efficiency/test-fact-ledger"
stackable = false
requires_human_merge = true
acceptance = "advisory per-test ledger reports each test's reachable owners, oracle kind/strength, observed values, and static limitations"
commands = [
  "cargo xtask test-efficiency-report",
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask reports index",
  "cargo xtask receipts check",
  "cargo xtask test-oracle-report",
  "cargo xtask check-static-language"
]

[[work_item]]
id = "test-efficiency/vacuous-signal-v1"
status = "done"
branch = "test-efficiency/vacuous-signal-v1"
stackable = false
requires_human_merge = true
acceptance = "advisory findings classify smoke-only, broad-oracle, disconnected, opaque, circular, and likely-vacuous test signals with reasons"
commands = [
  "cargo xtask test-efficiency-report",
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask fixtures",
  "cargo xtask goldens check",
  "cargo xtask check-static-language"
]

[[work_item]]
id = "test-efficiency/duplicate-discriminator-v1"
status = "done"
branch = "test-efficiency/duplicate-discriminator-v1"
stackable = false
requires_human_merge = true
acceptance = "advisory duplicate-discriminator groups name tests with the same owner, activation signature, and oracle shape; emit class `duplicative` and reason `duplicate_activation_and_oracle_shape`"
commands = [
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask fixtures",
  "cargo xtask goldens check",
  "cargo xtask check-static-language"
]

[[work_item]]
id = "test-efficiency/report-and-metrics"
status = "done"
branch = "test-efficiency/report-and-metrics"
stackable = false
requires_human_merge = true
acceptance = "test-efficiency reports include a stable `metrics` object with `tests_scanned`, per-class counts (including `duplicative`), per-reason counts (including `duplicate_activation_and_oracle_shape`), and `duplicate_discriminator_group_count` distinct from the duplicative-test count; capability metadata in `metrics/capabilities.toml` references the new metrics surface"
commands = [
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask reports index",
  "cargo xtask receipts check",
  "cargo xtask metrics",
  "cargo xtask check-capabilities"
]

[[work_item]]
id = "docs/badge-policy"
status = "done"
branch = "docs/badge-policy"
stackable = false
requires_human_merge = true
acceptance = "docs/BADGE_POLICY.md locks the badge counting rule, native JSON shape, Shields projection, and the exact test-efficiency vocabulary emitted by xtask::test_efficiency_report"
commands = [
  "cargo xtask test-efficiency-report",
  "cargo xtask markdown-links",
  "cargo xtask check-doc-index",
  "cargo xtask check-readme-state",
  "cargo xtask check-pr"
]

[[work_item]]
id = "badge/summary-renderer-v1"
status = "ready"
branch = "badge/summary-renderer-v1"
stackable = false
requires_human_merge = true
acceptance = "private BadgeSummary types in output::badge render badge-json and badge-shields projections from CheckOutput; cargo xtask check-public-api stays green"
commands = [
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask check-public-api",
  "cargo xtask check-output-contracts"
]

[[work_item]]
id = "badge/ripr-count-v1"
status = "blocked"
branch = "badge/ripr-count-v1"
stackable = false
requires_human_merge = true
blocked_by = ["badge/summary-renderer-v1"]
blocked_reason = "needs the private BadgeSummary renderer before exposing the badge formats on ripr check"
acceptance = "ripr check --format badge-json and --format badge-shields emit a single source-of-truth schema with snake_case fields and a mechanical Shields projection; counts include unsuppressed_exposure_gaps, suppressed_exposure_gaps, unknowns, analyzed_findings, analyzed_tests"
commands = [
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask fixtures",
  "cargo xtask goldens check",
  "cargo xtask check-output-contracts"
]

[[work_item]]
id = "test-intent/v1"
status = "ready"
branch = "test-intent/v1"
stackable = false
requires_human_merge = true
acceptance = ".ripr/test_intent.toml loader marks declared tests intentional in target/ripr/reports/test-efficiency.json; supported intents are smoke, business_case_duplicate, opaque_external_oracle, integration_contract, performance_guard, documentation_example; declared tests stay visible in reports"
commands = [
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask test-efficiency-report",
  "cargo xtask fixtures",
  "cargo xtask goldens check"
]

[[work_item]]
id = "badge/ripr-plus-count-v1"
status = "blocked"
branch = "badge/ripr-plus-count-v1"
stackable = false
requires_human_merge = true
blocked_by = ["badge/ripr-count-v1", "test-intent/v1"]
blocked_reason = "needs both the ripr badge counter and test-intent loader before adding the stricter ripr+ badge"
acceptance = "ripr check --format badge-plus-json and --format badge-plus-shields sum unsuppressed exposure gaps and unsuppressed actionable test-efficiency findings (excluding declared intent); intentional findings are reported in counts.intentional_test_efficiency_findings"
commands = [
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask fixtures",
  "cargo xtask goldens check",
  "cargo xtask check-output-contracts"
]

[[work_item]]
id = "suppressions/v1"
status = "blocked"
branch = "suppressions/v1"
stackable = false
requires_human_merge = true
blocked_by = ["badge/ripr-plus-count-v1"]
blocked_reason = "needs the ripr+ counter before suppressions can affect a published badge number"
acceptance = ".ripr/suppressions.toml loader; reason and owner required; expiry encouraged; suppressed findings remain visible in reports; badge counts.suppressed_* reflect the count"
commands = [
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask fixtures",
  "cargo xtask goldens check"
]

[[work_item]]
id = "ci/badge-artifacts"
status = "blocked"
branch = "ci/badge-artifacts"
stackable = false
requires_human_merge = true
blocked_by = ["badge/ripr-plus-count-v1", "suppressions/v1"]
blocked_reason = "needs both badge formats and durable intent/suppression handling before CI publishes badge artifacts that consumers may treat as meaningful"
acceptance = "CI uploads target/ripr/reports/ripr-badge*.json and ripr-plus-badge*.json, links them from the report index, and shows counts in the GitHub step summary; advisory by default; --fail-on-nonzero is opt-in"
commands = [
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask reports index",
  "cargo xtask receipts",
  "cargo xtask receipts check"
]

[[work_item]]
id = "badge/publish-main-endpoint"
status = "blocked"
branch = "badge/publish-main-endpoint"
stackable = false
requires_human_merge = true
blocked_by = ["ci/badge-artifacts"]
blocked_reason = "publishing a Shields endpoint from main requires stable badge artifacts and a network-allowlist entry"
acceptance = "Shields-compatible badge JSON published from main only; README references the endpoint; policy/network_allowlist.txt updated; PR workflows do not publish"
commands = [
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask check-network-policy",
  "cargo xtask check-workflows"
]
