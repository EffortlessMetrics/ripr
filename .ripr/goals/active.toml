id = "syntax-backed-analyzer-foundation"
title = "Move ripr from lexical facts to syntax-backed facts"
status = "active"

objective = """
Move the analyzer from line-oriented scanner facts to a stable fact model and
syntax adapter boundary without changing current analyzer behavior first.
"""

end_state = [
  "FileFacts model exists",
  "syntax adapter boundary exists",
  "tests and oracles are extracted from syntax-backed facts",
  "probes attach to stable owner symbols",
  "current probe families are generated from syntax facts"
]

[[work_item]]
id = "analysis/file-facts-model"
status = "done"
branch = "analysis/file-facts-model"
stackable = false
requires_human_merge = true
acceptance = "current scanner behavior is preserved while analysis consumes FileFacts DTOs"
commands = [
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask fixtures",
  "cargo xtask goldens check"
]

[[work_item]]
id = "analysis/syntax-adapter-mvp"
status = "done"
branch = "analysis/syntax-adapter-mvp"
stackable = false
requires_human_merge = true
acceptance = "parser adapter boundary exists while the current lexical adapter remains behavior-compatible"
commands = [
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask fixtures",
  "cargo xtask goldens check"
]

[[work_item]]
id = "analysis/ast-test-oracle-extraction"
status = "ready"
branch = "analysis/ast-test-oracle-extraction"
stackable = false
requires_human_merge = true
acceptance = "syntax-backed facts identify tests and assertion/oracle shapes without output drift unless documented"
commands = [
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask fixtures",
  "cargo xtask goldens check"
]

[[work_item]]
id = "analysis/ast-probe-ownership"
status = "blocked"
branch = "analysis/ast-probe-ownership"
stackable = false
requires_human_merge = true
acceptance = "diff spans map to stable owner symbols without cross-linking duplicate names"
blocked_by = [
  "analysis/syntax-adapter-mvp"
]
blocked_reason = "requires syntax node and owner facts"
commands = [
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask fixtures",
  "cargo xtask goldens check"
]

[[work_item]]
id = "analysis/ast-probe-generation"
status = "blocked"
branch = "analysis/ast-probe-generation"
stackable = false
requires_human_merge = true
acceptance = "current probe families are generated from syntax facts with fixture-backed evidence"
blocked_by = [
  "analysis/ast-probe-ownership",
  "analysis/ast-test-oracle-extraction"
]
blocked_reason = "requires ownership and syntax-backed oracle facts"
commands = [
  "cargo xtask shape",
  "cargo xtask fix-pr",
  "cargo xtask check-pr",
  "cargo xtask pr-summary",
  "cargo xtask fixtures",
  "cargo xtask goldens check"
]
