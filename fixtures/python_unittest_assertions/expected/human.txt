ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_unittest_assertions/input

Summary: 2 probe(s), 1 exposed, 0 weak, 0 unrevealed, 1 no path, 0 unknown

INFO src/service.py:2

Changed
  after:      return value.strip().lower()

Probe
  family: return_value
  delta:  value

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  exposed (info, confidence 0.55)

Evidence
  - reach yes: 1 related Python test(s) found for owner `normalize`
  - infection unknown: Python preview adapter does not yet model infection.
  - propagation unknown: Python preview adapter does not yet model propagation.
  - observation yes: Strongest extracted Python oracle kind: `exact_value` (rank 5)
  - discriminator yes: Related Python test uses a `exact_value` oracle; preview static evidence treats it as a discriminator.
  - related test tests/test_service.py:7 test_normalize_exact_value uses strong exact value oracle: self.assertEqual(normalize(" PAID "), "paid") at line 8 (exact_value/strong)

Weakness
  - Related Python test reaches `normalize` with a `exact_value` oracle. Preview static evidence suggests the changed behavior is observed by a strong assertion.

Next step
  Python preview: changed behavior is observed under a strong oracle; verify the assertion targets the changed boundary value.

WARNING src/service.py:5

Changed
  after:      if amount < 0:

Probe
  family: predicate
  delta:  control

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  no_static_path (warning, confidence 0.40)

Evidence
  - reach no: 0 related Python test(s) found for owner `reject_negative`
  - infection unknown: Python preview adapter does not yet model infection.
  - propagation unknown: Python preview adapter does not yet model propagation.
  - observation no: Strongest extracted Python oracle kind: `unknown` (rank 0)
  - discriminator no: Python preview found no strong discriminator; use exact-value or exact-error assertions to escalate.
  - owner: reject_negative
  - owner_kind: function

Weakness
  - No Python test references `reject_negative(`; add a pytest or unittest test that calls the changed owner.

Next step
  Python preview: no related test calls the changed owner; add a pytest or unittest test that exercises this behavior.
