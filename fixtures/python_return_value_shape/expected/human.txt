ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_return_value_shape/input

Summary: 1 probe(s), 0 exposed, 1 weak, 0 unrevealed, 0 no path, 0 unknown

WARNING src/rounding.py:2

Changed
  after:      return round(amount * 100) / 100

Probe
  family: return_value
  delta:  value

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  weakly_exposed (warning, confidence 0.40)

Evidence
  - reach yes: 1 related Python test(s) found for owner `round_cents`
  - infection unknown: Python preview adapter does not yet model infection.
  - propagation unknown: Python preview adapter does not yet model propagation.
  - observation weak: Strongest extracted Python oracle kind: `smoke_only` (rank 2)
  - discriminator weak: Python preview adapter found no strong discriminator; use `assert ... == ...` or `self.assertEqual(...)` to escalate.
  - related test tests/test_rounding.py:1 test_round_cents_smoke uses smoke smoke only oracle: assert result

Weakness
  - Related Python test reaches `round_cents` but the strongest extracted oracle is `smoke_only`; add or verify an exact-value assertion to make the preview finding stronger.

Next step
  Python preview: add or verify a focused exact-value assertion (`assert ... == ...` or `self.assertEqual(...)`) for the changed behavior.
