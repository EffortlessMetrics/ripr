ripr static RIPR exposure analysis
mode: fast
root: fixtures/boundary_discriminator/input

Summary: 1 probe(s), 0 exposed, 0 weak, 0 unrevealed, 0 no path, 1 unknown

WARNING fixtures/boundary_discriminator/input/src/lib.rs:2

Static exposure: infection_unknown (predicate, control)

Changed behavior:
  before: if amount > discount_threshold {
  after:  if amount >= discount_threshold {

RIPR:
  Reach:       yes — Related tests appear to reach discounted_total: at_threshold_discounts, below_threshold_has_no_discount, far_above_threshold_discounts
  Infect:      unknown — Predicate changed, but no literal boundary was visible in the changed expression
  Propagate:   weak — Changed behavior may propagate through a constructed value or field
  Observe:     yes — A related test observes a value or effect near the changed behavior
  Discriminate: yes — Strong oracle found: exact value or pattern assertion

Related tests / oracles:
  - tests/pricing.rs:9 at_threshold_discounts [strong] — assert_eq!(discounted_total(100, 100), 90);
  - tests/pricing.rs:4 below_threshold_has_no_discount [strong] — assert_eq!(discounted_total(90, 100), 90);
  - tests/pricing.rs:14 far_above_threshold_discounts [strong] — assert_eq!(discounted_total(200, 100), 180);

Gap:
  - No detected boundary input for the changed predicate
  - Predicate changed, but no literal boundary was visible in the changed expression

Stop reasons:
  - infection_evidence_unknown

Recommended next step:
  Add a targeted boundary or negative-path test, or teach ripr about the fixture/builder in ripr.toml.
