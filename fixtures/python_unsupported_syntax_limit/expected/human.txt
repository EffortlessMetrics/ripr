ripr static RIPR exposure analysis
mode: fast
root: fixtures/python_unsupported_syntax_limit/input

Summary: 1 probe(s), 1 exposed, 0 weak, 0 unrevealed, 0 no path, 0 unknown

INFO src/stream.py:2

Changed
  after:      yield amount - 10

Probe
  family: predicate
  delta:  control

Language
  language: python
  status: preview
  owner kind: function

Static exposure
  exposed (info, confidence 0.60)

Evidence
  - reach yes: 1 related Python test(s) found for owner `emit_amount`
  - infection unknown: Python preview adapter does not yet model infection.
  - propagation unknown: Python preview adapter does not yet model propagation.
  - observation yes: Strongest extracted Python oracle kind: `exact_value` (rank 5)
  - discriminator yes: Related Python test uses a `exact_value` oracle; static evidence suggests the changed behavior is discriminated.
  - related test tests/test_stream.py:1 test_emit_amount uses strong exact value oracle: assert list(emit_amount(10)) == [10]

Weakness
  - Related Python test reaches `emit_amount` with a `exact_value` oracle. Static evidence suggests the changed behavior is observed under an exact-value discriminator.
  - Static limit `unsupported_syntax`: This Python syntax shape is parsed but not modeled by the preview evidence extractor.

Next step
  Python preview: changed behavior is observed under a strong oracle; verify the assertion targets the changed boundary value.
