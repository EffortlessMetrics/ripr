# Normalize line endings. Text files use LF in the repository and in the
# working tree on every platform. This prevents editors/tools on Windows from
# rewriting a file to CRLF (or mixed) endings, which otherwise shows the entire
# file as changed in a diff. Binary files are auto-detected and left untouched.
* text=auto eol=lf

# Explicit binary types (guards against text auto-detection mistakes).
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.pdf  binary
*.vsix binary
*.woff  binary
*.woff2 binary

# Diff fixtures are diff *data*: a trailing space on a context or added line is
# part of the payload under test, not a lint violation. Markdown uses a trailing
# double space as a hard line break. Whitespace linting does not apply to either,
# and the source/W7 join surfaces the whole frozen W7 range to `git diff --check`.
*.patch -whitespace
*.diff  -whitespace
*.md    -whitespace
